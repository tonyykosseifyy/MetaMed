import { View, Text } from 'react-native'
import React from 'react'
import Background from '../components/Background'

export default function Transcripts() {
  return (
 <Background title='Transcripts'></Background>
  )
}